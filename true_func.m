% Define your objective function ans constraints here
% objective functions are minimized by default
% "g(x)>=0" constraints, by default, type g(x)
%
function f = true_func(x, problem,ncon) % add a counter!!!


        f = optimize_type2(x);
        
  
    
end